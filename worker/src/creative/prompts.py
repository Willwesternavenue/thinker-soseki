"""創作モードのプロンプト定義。

既存実装にはプロンプトの版管理が無いため、創作系では PROMPT_VERSIONS を持ち
creative_traces へ保存する(仕様§14 / T1設計書 §7)。
**プロンプト本文を変更したら、同じコミットで対応する version を上げること。**
"""

# ステップ名 → プロンプト版。creative_traces.prompt_versions に保存する
PROMPT_VERSIONS = {
    "brief": "v1",
    "sources": "v1",
    # 承認済み Bridge Rule の節を追加(引き継ぎB-1。橋が無ければ空文字で消える)
    "outline": "v2",
    "draft": "v1",
    "guard_judge": "v2",  # 過検出を抑えるため判定の原則を明記(C-T6の実運用で改訂)
}

# 全生成プロンプトに共通で入れる禁止事項(仕様§14)。
# 個別プロンプトから参照して重複を避ける。
COMMON_CONSTRAINTS = """## 厳守事項
- 原作者本人として名乗らない。原作者本人が書いたと誤認させる表現を使わない
- 原典を長く引用しない。既存作品の改作をしない
- 出典のない有名句を作らない
- 象徴の意味を本文中で解説しない。創作規則そのものを本文に書かない
- 指定されたプロファイル以外の作家の特徴を混ぜない
- 参照した創作カードの内容を本文へそのまま露出させない"""

BRIEF_SYSTEM = """あなたは創作依頼を構造化するアシスタントである。
利用者の自由記述から、後続の生成が使える形へ項目を整理する。
書かれていない項目を創作して埋めない(不明なら null)。JSONのみを出力する。"""

BRIEF_PROMPT = """以下は創作の依頼内容である。項目を構造化せよ。

## 依頼内容
{brief_raw}

## 出力形式(JSONのみ)
{{
  "motif": "中心となるモチーフ(例: 鏡)",
  "situation": "物語の核となる状況",
  "emotional_target": "目指す読後感",
  "period": "時代設定(不明なら null)",
  "length": 目標文字数(整数。不明なら null),
  "constraints": ["追加制約", "..."]
}}"""

SOURCES_SYSTEM = """あなたは創作の参考にする原典を選ぶアシスタントである。
与えられた候補の中から、依頼内容と関連の深いものを選ぶ。
候補に無いものを挙げない。JSONのみを出力する。"""

SOURCES_PROMPT = """新作を書くにあたり、参考にする原典を候補から選べ。

## 依頼内容
- モチーフ: {motif}
- 状況: {situation}
- 読後感: {emotional_target}

## 候補(原典の章)
{candidates}

## 指示
- 関連の深い順に最大 {max_count} 件を選ぶ
- 選んだ章の筋をなぞった作品を書くためではなく、作風を掴むために使う
- 候補に無い名称を出力しない

## 出力形式(JSONのみ)
{{
  "selected": ["章の名称", "..."],
  "reason": "選定理由(簡潔に)"
}}"""

OUTLINE_SYSTEM = """あなたは短編小説の構成(outline)を考える創作アシスタントである。
承認済みの創作カードと参考原典の作風を踏まえ、全文を書く前の設計図を作る。
出典のない有名句を作らず、象徴の意味を解説する構成にしない。JSONのみを出力する。"""

OUTLINE_PROMPT = """新作の構成(outline)を考えよ。

## 依頼内容
- モチーフ: {motif}
- 状況: {situation}
- 目指す読後感: {emotional_target}
- 追加制約: {constraints}

## 承認済み創作カード(必ず踏まえる)
{cards}

{bridges}

## 参考原典(作風を掴むために使う。筋書きを複製しない)
{source_excerpt}

## 出力形式(JSONのみ)
{{
  "intro": "導入(どう始まるか)",
  "anomaly": "中心となる異常(何が一つだけ異常か)",
  "repetition_and_change": "反復とその変化",
  "turn": "転換(何が反転・発見されるか)",
  "ending": "終結(どう終わるか。教訓を説明しない)",
  "unexplained": "説明せず残す要素"
}}"""

DRAFT_SYSTEM = (
    """あなたは指定された作風で短編小説の本文を書く創作アシスタントである。
承認済みの創作カードと構成(outline)に忠実に、指定の文字数・正書法で本文を書く。

"""
    + COMMON_CONSTRAINTS
)

DRAFT_PROMPT = """以下の構成に基づき、本文を書け。

## 依頼内容
- モチーフ: {motif}
- 目標文字数: {length}
- 正書法: {orthography_policy}
- 追加制約: {constraints}

## 構成(outline)
- 導入: {intro}
- 中心となる異常: {anomaly}
- 反復と変化: {repetition_and_change}
- 転換: {turn}
- 終結: {ending}
- 説明せず残す要素: {unexplained}

## 承認済み創作カード(必ず踏まえる)
{cards}

## 参考原典(作風を掴むために使う。筋書きを複製しない)
{source_excerpt}

## 出力形式(JSONのみ)
{{
  "text": "本文全体"
}}"""

GUARD_JUDGE_SYSTEM = """あなたは創作物が禁止事項に違反していないかを判定する検査者である。
与えられた禁止事項それぞれについて、本文が違反しているかを判定する。

判定の原則:
- 本文に**実際に書かれている記述だけ**を根拠にする。推測で違反を作らない
- 各禁止事項に「違反にあたらない形」が示されている場合、それに該当する書き方は
  違反としない
- 比喩・象徴・並置は、因果や仕組みの**説明ではない**。説明と見なすのは、
  地の文や台詞で理由・原理・意味を明示的に述べている場合に限る
- 語り手が何かに気づく描写は、それ自体では説明にあたらない

JSONのみを出力する。"""

GUARD_JUDGE_PROMPT = """以下の本文が、禁止事項に違反していないか判定せよ。

## 禁止事項
{prohibitions}

## 本文
{draft}

## 出力形式(JSONのみ。違反が無ければ violations は空配列)
{{
  "violations": [
    {{"card_id": "違反した禁止事項のID", "reason": "本文のどこがどう違反しているか"}}
  ]
}}"""
